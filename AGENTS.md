# CHUGUMI 작업 지침

## 제품과 현재 구조

- CHUGUMI의 부제는 **추구미 관리하는 남자**다. 사용자의 정체성을 유지하며 상황과 취향에 맞는 스타일을 제안한다.
- 현재 구현과 제약은 `docs/feedback-and-evaluation.md`, 코드 스타일은 `docs/code-conventions.md`를 따른다. 과거 계획을 구현 완료의 근거로 삼지 않는다.
- `src/domain`: 모델과 순수 규칙. Next.js, OpenAI, 파일 시스템에 의존하지 않는다.
- `src/application`: 유스케이스와 입력 검증. 외부 I/O는 포트로 받는다.
- `src/infrastructure`: OpenAI, 저장소, 인증, 의존성 조립.
- `src/app`: 페이지와 HTTP 경계. `src/presentation`: UI, Hook, 클라이언트 상태.
- 기존 책임 경계를 유지하되 단순한 코드에 불필요한 인터페이스와 클래스를 추가하지 않는다.

## 작업 방식

1. 요청의 결과와 완료 조건을 짧게 정리하고, 관련 코드와 기존 변경 사항을 확인한다.
2. 구현 요청이면 구현과 검증까지 진행한다. 이미 정해진 범위에서 반복 승인이나 계획만 제시하며 멈추지 않는다.
3. 여러 경계를 바꾸는 작업은 `docs/task-template.md` 형식으로 짧은 계획과 검증 근거를 남긴다. 작은 수정에는 계획 문서를 만들지 않는다.
4. 기존 사용자 변경을 보존한다. 관련 없는 포맷 변경, 의존성 업데이트, 생성 파일 정리를 함께 섞지 않는다.
5. 한국어로 결과, 검증, 남은 제약을 보고한다. 실행하지 않은 테스트나 API 호출을 성공했다고 쓰지 않는다.

## 가독성과 UI

- Prettier와 ESLint 설정을 사용한다. 선언과 분기를 한 줄에 몰아넣지 않고, 책임이 바뀌는 곳에 빈 줄을 둔다.
- 축약어보다 역할이 드러나는 이름을 쓴다. 코드 내용을 되풀이하는 주석은 추가하지 않는다.
- UI는 사용자가 이해하는 말로 작성한다. 내부 모델·실험 구현 용어, 확인되지 않은 효과, 가짜 진행률을 노출하지 않는다.
- Next.js 동작을 변경할 때 설치된 버전에 포함된 `node_modules/next/dist/docs/`의 관련 문서를 먼저 확인한다. 없으면 해당 버전의 공식 문서를 확인한다.
- Next.js가 추가한 안내 블록이 있으면 보존하며 이 프로젝트 지침을 삭제하지 않는다.

## 데이터와 비밀키

- 실제 환경 파일, 인증 파일, 키 값을 출력하거나 문서·테스트·로그에 복사하지 않는다. 존재 여부와 변수명만 확인한다.
- 서버 비밀을 `NEXT_PUBLIC_*`, 클라이언트 모듈, 응답 DTO에 포함하지 않는다.
- 리뷰 응답에 세션 ID, 원본 해시, 사진 데이터를 추가하지 않는다. 사용자 사진을 테스트 fixture로 저장하지 않는다.
- 기본 검증은 외부 API를 모킹한다. 실제 유료 생성·평가는 해당 실행이 요청되었거나 이미 승인된 범위에서만 수행한다.

## 검증과 완료 기준

- 문서만 변경: 링크와 명령의 정확성을 확인한다.
- 코드 변경: 관련 회귀 사례를 확인하고 `npm run check`를 실행한다. 단순 문구·포맷 변경에 구현을 복제하는 테스트를 추가하지 않는다.
- API·라우팅·빌드 설정·의존성 변경 및 제출 전: `npm run verify`를 실행한다. check와 webpack 프로덕션 빌드를 순서대로 실행한다.
- UI 동작 변경: 브라우저에서 실제 입력, 성공·실패 상태와 관련 상호작용을 확인한다. 브라우저를 사용할 수 없으면 미검증으로 명시한다.
- 실패하면 원인을 수정하고 관련 검증을 다시 실행한다. 환경 때문에 막힌 검증은 실패 원인과 미확인 범위를 기록한다.
- `npm run eval`은 실제 API를 호출할 수 있는 별도 실험이다. 일반 검증 명령에 포함하지 않는다.

## Code Review Rules

- 생성·피드백에서 세션 소유권 검사를 우회하지 않는지 확인한다.
- 동일 eventId의 재전송이 중복 기록되지 않는지, 새 의견이 최신 의견으로 반영되는지 확인한다.
- 다운로드·품질 신고·비교를 선호도 보상에 합치지 않는다. 평가 실패가 성공한 이미지 생성을 실패로 바꾸지 않는지 확인한다.
- 파일 저장소의 원자적 쓰기와 단일 프로세스 한계를 유지·명시한다. 다중 인스턴스에서 안전하다고 주장하지 않는다.
- 자동 평가와 사용자 선호를 구분한다. 모델 판정을 사람 검증 결과나 입증된 품질 개선으로 표현하지 않는다.
